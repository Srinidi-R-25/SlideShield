{
  "name": "slideshield-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dev.bat",
    "start:backend": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000",
    "start:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
